<h1>Editing Book</h1>
<%= form_with model: @book do |f| %>
 <label for="editTitle">Title</label>
 <%= f.text_field :title %>
 <label for="editOpinion">Opinion</label>
 <%= f.text_area :body %>
 <%= f.submit "Update Book" %>
<% end %>
